<h1>Tree Models</h1>
 
<h2>Add a new model: <%= link_to 'New Tree', new_tree_path %></h2>

<table>
  <tr>
    <th>Domain</th>
    <th>Accuracy</th>
		<th>Actions</th>
    <th colspan="3"></th>
  </tr>
 
<% @trees.each do |tree| %>
  <tr>
    <td><%= tree.pr_id %></td>
    <td><%= tree.accuracy %></td>
    <td><%= link_to 'HTML', tree_path(tree) %></td>
		<td><%= link_to 'JSON', polymorphic_path( tree, :format => :json ) %></td>
    <td> --- <%= link_to 'Edit', edit_tree_path(tree) %></td>
  </tr>
<% end %>
</table>
