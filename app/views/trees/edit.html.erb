<h1>Editing tree metadata</h1>
 
<%= form_for @tree, :url => trees_path, :html => { :multipart => true } do |f| %>
  <% if @tree.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@tree.errors.count, "error") %> prohibited
      this tree metadata from being saved:</h2>
    <ul>
    <% @tree.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
  <p>
    <%= f.label :author %><br>
    <%= f.text_field :author %>
  </p>
 
  <p>
    <%= f.label :accuracy %><br>
    <%= f.text_area :accuracy %>
  </p>


<h2> Add the Model File: </h2>

		<%= f.file_field :model %> </br>
		<%= f.submit %>
<% end %>



<%= link_to 'Back', trees_path %>

